name: CI

on: 
  push:
    branches:
      - main


jobs:
  test:
    runs-on: ubuntu-lastest

    steps: 
    - name: Checkout repository
      uses: actions/checkout@v2

    -name: Install dependencies
      run: npm install

    -name: Run tests
      run: npm test
